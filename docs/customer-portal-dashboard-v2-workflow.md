# Unified Request Routing

Dashboard quick actions use the unified request entry point. Service-specific actions pass a type hint for emergency maintenance, quotation, spare parts, or technical consultation so the unified request flow can preselect the requested service without creating separate legacy forms.
