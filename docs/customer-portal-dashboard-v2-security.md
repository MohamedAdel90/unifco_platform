# Access Boundary

The dashboard does not broaden authorization. It renders data supplied through the existing authenticated customer portal scope. Authorization remains server-side; UI visibility is not an authorization boundary.
