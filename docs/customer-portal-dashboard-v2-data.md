# Dashboard Data Behavior

The view consumes existing server-provided customer workspace collections and metrics. Fallback calculations are derived from the same customer-scoped collections when a summary metric is absent. No cross-customer queries are introduced in the view.
