Recover production application service and deploy the final public ticket issuance fix from current main. Verify public request submission commits the ticket/reference before downstream conversion and redirects to the receipt even when CRM/work-order conversion fails. Confirm ticket prefixes UNQ-/UNM-/UNRM-/UNUM-/UNC-, sequential public_request_counters usage, and successful receipt rendering. Preserve the production-compatible shared homepage-style request header with centered red Request Service / طلب خدمة action using id requestCenterAction, camera attachments, hero copy, compact request choices, 220px map, quotation labels, bilingual parity, one-page form, migration 000049, dynamic branding, Supervisor state, and port 8081 readiness.