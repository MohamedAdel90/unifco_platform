Recover production application service and deploy the camera-first public request attachment experience. Verify /request-service and /request-service?lang=en keep the balanced homepage-style header: UNIFCO logo plus language switch on the left, centered red Request Service / طلب خدمة button, and seven icon navigation items on the right without overflow. Verify the request page HTML includes the public-request-camera-attachments and public-request-camera-attachments-logic markers, camera capture uses capture=environment, and the bilingual UI offers التقاط صورة / Take Photo plus اختيار من الجهاز / Choose from Device. Verify equipment photos, problem photos and previous reports support camera/device selection, client-side previews, file counts and per-file removal before submit while preserving the existing upload field names. Preserve the blue hero copy, compact request choices, 220px map, quotation-oriented field labels, bilingual parity, one-page form, ticket prefixes UNQ-/UNM-/UNRM-/UNUM-/UNC-, migration 000049, receipt page, dynamic branding, Supervisor state, and port 8081 readiness.