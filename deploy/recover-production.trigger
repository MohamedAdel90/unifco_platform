Recover production application service and deploy resilient public ticket issuance. Verify public request submission always commits the ticket/reference first and redirects to the receipt even if downstream CRM/work-order conversion throws an exception. Confirm ticket prefixes remain UNQ-/UNM-/UNRM-/UNUM-/UNC- with serials from public_request_counters, the receipt route loads the issued reference, and downstream conversion failures are logged rather than returned to the customer as HTTP 500. Preserve the shared homepage-style request header with centered red Request Service / طلب خدمة action, camera attachments, hero copy, compact request choices, 220px map, quotation labels, bilingual parity, one-page form, migration 000049, dynamic branding, Supervisor state, and port 8081 readiness.