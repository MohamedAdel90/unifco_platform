Retry production recovery for the exact homepage-matched request brand typography and geometry after the latest SSH timeout. Verify /request-service and /request-service?lang=en load Inter explicitly and use the same brand-link, site-logo-frame, site-logo and brand-copy structure as the homepage, with desktop values matching exactly: logo frame 42x50, logo image 42x65, UNIFCO wordmark 24px Inter weight 800, ONE FACILITY SHOP 6px Inter weight 900, language control 42px high with 6px radius. Verify no desktop breakpoint reduces the wordmark size. Keep logo and language in the same left-side alignment and spacing, centered red Request Service / طلب خدمة button, and seven icon navigation items on the right without overflow. Preserve camera attachments, hero copy, compact request choices, 220px map, quotation labels, bilingual parity, one-page form, ticket prefixes UNQ-/UNM-/UNRM-/UNUM-/UNC-, migration 000049, receipt page, dynamic branding, Supervisor state, and port 8081 readiness.