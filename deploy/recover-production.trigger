Recover production application service and deploy the qualified final public request page header and layout. Verify /request-service and /request-service?lang=en render a balanced homepage-style white header constrained to the page width: UNIFCO logo plus language switch aligned on the LEFT in both languages, a centered red Request Service / طلب خدمة button with white text, and the seven icon navigation items aligned on the RIGHT without overflow. Verify the blue hero contains no UNIFCO · ONE FACILITY SHOP eyebrow and shows خدمة أسرع تبدأ بطلب أوضح / A faster service starts with a clearer request with the smaller explanatory sentence. Verify request type cards are compact; quotation and maintenance each reveal exactly two subtype buttons whose combined width equals the corresponding primary card width; Technical Consultation has no visible subtype row. Verify map height is 220px. Verify quotation mode changes technical labels to quotation-oriented item/equipment, manufacturer, model/part number, quotation category and specifications; other modes remain service-oriented. Preserve bilingual parity, one-page form, ticket prefixes UNQ-/UNM-/UNRM-/UNUM-/UNC-, migration 000049, attachments, receipt page, dynamic branding, Supervisor state, and port 8081 readiness.