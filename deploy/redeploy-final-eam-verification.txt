Final verification trigger after fixing deploy CSRF syntax. Run full EAM preflight, migrations, health/reorder commands, restart, homepage verification and login CSRF verification. Brand materialization remains skipped by request.

Phase 3 final verification retry after transient SSH timeout on 2026-08-22. Re-run full preflight and production deployment, then capture fresh live screenshots if deploy succeeds.

Sidebar production redeploy trigger: deploy main SHA 091638d9b7f3bff5e1e65764367ffac99f135090 and verify the redesigned grouped navigation is live.

Complete navigation verification: deploy the current main containing operational wave routes, dedicated workspace pages, complete sidebar/sub-tab coverage on every authenticated page, bilingual navigation, and SidebarNavigationTest coverage. Verify workspace/asset-360 and operational route registration before production restart.

User administration production upgrade: replace /workspace/users placeholder with real tenant-scoped User Management including KPIs, search, role/status/organization filters, user table, permissions/audit links, admin-only access, and feature-test coverage. Deploy and verify /workspace/users on production.

Full User Administration CRUD release: deploy current main after focused qualification success. Includes create/edit/profile, activate/deactivate/suspend, reset password, role/organization/employee assignment, tenant-scoped user permission overrides enforced by AuthorizationService, + New User UX, and security/tenant-isolation tests.

Enterprise User Administration security release: deploy the qualified main containing Last Login tracking, real lock state, password-change flags, revocable sessions, unified row Actions menu, bulk status actions, CSV import/export, user audit timeline, per-user API token revocation, and truthful MFA status. Qualification result must be success before production restart.

Compact User Profile permission UX: deploy current main SHA containing a fixed-height Role Permissions viewport, two-column module groups, all groups collapsed by default, internal scrolling, and search-driven expansion. Verify the overall User Profile no longer grows vertically with permission count.

HR Phase 1 release: deploy current qualified main containing HR Command Center, expanded Saudi-ready employee master, Employee 360, employment contract snapshots, employee document/expiry tracking, compensation master data in SAR, redesigned Employee Directory and modernized HR Operations UI. Apply migration 2026_08_23_000037, verify /hr/dashboard, /hr/employees and Employee 360 routes after restart. Qualification must remain result=success and Wave10 HR regression tests must remain green.
