@php($customerInstallations=\App\Models\AssetPartInstallation::with(['item','removedItem','workOrder'])->where('asset_id',$asset->id)->latest('installed_at')->limit(50)->get())
<section class="box" style="margin-top:14px"><h3>Installed Parts & Components · قطع الغيار المركبة</h3><div class="table-wrap"><table class="table"><thead><tr><th>Date</th><th>Work Order</th><th>Part</th><th>Description</th><th>Quantity</th><th>Removed Component</th></tr></thead><tbody>@forelse($customerInstallations as $installation)<tr><td>{{ $installation->installed_at?->format('Y-m-d') }}</td><td>@if($installation->workOrder)<a class="btn secondary" href="{{ route('customer.work-orders.show',$installation->workOrder) }}">{{ $installation->workOrder->work_order_no }}</a>@else—@endif</td><td>{{ $installation->item?->item_code }}</td><td>{{ $installation->item?->name }}</td><td>{{ (float)$installation->quantity }}</td><td>{{ $installation->removedItem?->item_code ?: '—' }}</td></tr>@empty<tr><td colspan="6">No installed part history recorded.</td></tr>@endforelse</tbody></table></div><p style="font-size:9px;color:var(--muted);margin-top:9px">Internal purchasing cost, supplier pricing and inventory valuation are not displayed in the customer portal.</p></section>
