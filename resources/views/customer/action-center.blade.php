@php
    $ar = app()->getLocale() === 'ar';
    $filters = [
        'all' => [$ar ? 'الكل' : 'All', $total],
        'quotations' => [$ar ? 'عروض الأسعار' : 'Quotations', $quotations->count()],
        'work' => [$ar ? 'استلام الأعمال' : 'Work acceptance', $workOrders->count()],
        'invoices' => [$ar ? 'الفواتير' : 'Invoices', $invoices->count()],
        'renewals' => [$ar ? 'التجديدات' : 'Renewals', $contracts->count()],
        'messages' => [$ar ? 'الرسائل' : 'Messages', $unread],
    ];
@endphp

@include('customer.partials.portal-shell-open', [
    'activeSection' => 'actions',
    'pageTitle' => $ar ? 'الإجراءات المطلوبة' : 'Action Required From You',
    'pageDescription' => $ar ? 'قرارات ومتابعات مرتبة حسب الاستعجال وتاريخ الاستحقاق.' : 'Decisions and follow-ups prioritized by urgency and due date.',
])

@push('late-styles')
<style>
    /* Approved Action Required presentation: match the customer dashboard visual language exactly. */
    .portal-main{padding:0 14px 30px}.portal-topbar{height:46px;margin:0 -14px;padding:0 18px;justify-content:flex-end}.portal-topbar .portal-heading{display:none}.portal-content{max-width:none;margin:0;padding-top:8px}.portal-breadcrumb,.portal-page-head{display:none}
    .portal-sidebar .portal-account{display:none}.portal-sidebar:before{content:"UNIFCO\A Customer Portal";white-space:pre;min-height:54px;margin:0 6px 8px;padding:5px 4px 5px 53px;display:flex;align-items:center;background:url("{{ asset('images/unifco-logo.webp') }}") no-repeat 5px 50%/40px auto;color:#fff;font-size:11px;font-weight:850;line-height:1.3;border-bottom:1px solid #ffffff1a}.portal-sidebar .portal-create{margin-top:5px}
    .action-hero{position:relative;min-height:132px;margin-bottom:10px;overflow:hidden;border:1px solid #d9e6f3;border-radius:12px;background:linear-gradient(90deg,#f8fbff 0%,#edf5fd 43%,#082d63 100%);box-shadow:var(--shadow)}.action-hero:after{content:"";position:absolute;inset:0;background:linear-gradient(90deg,rgba(248,251,255,.99) 0%,rgba(248,251,255,.96) 34%,rgba(248,251,255,.52) 52%,rgba(7,45,96,.08) 74%,rgba(3,29,73,.14) 100%);pointer-events:none}.action-hero-bg{position:absolute;inset:0 0 0 42%;width:58%;height:100%;object-fit:cover;object-position:center;opacity:1}.action-hero-main{position:relative;z-index:2;padding:13px 300px 12px 18px;min-height:132px;display:flex;flex-direction:column;justify-content:space-between}.action-hero-title{display:flex;align-items:flex-start;gap:12px}.action-hero-title-icon{position:relative;width:44px;height:44px;border-radius:50%;display:grid;place-items:center;background:#eaf3ff;color:var(--blue);flex:0 0 auto}.action-hero-title-icon .ui-icon{width:24px;height:24px}.action-hero-badge{position:absolute;inset-block-start:-5px;inset-inline-end:-4px;min-width:20px;height:20px;padding:0 5px;border-radius:10px;display:grid;place-items:center;background:var(--red);color:#fff;font-size:8px;font-weight:900;border:2px solid #fff}.action-hero-copy h2{font-size:24px;line-height:1.06;margin:1px 0 4px;letter-spacing:-.025em}.action-hero-copy p{font-size:10px;color:#4f637f;margin:0}.action-hero-details{display:flex;align-items:center;gap:0;flex-wrap:wrap;margin-top:11px}.action-hero-detail{display:flex;align-items:center;gap:9px;padding:0 21px;border-inline-end:1px solid #cdd9e7;min-height:36px}.action-hero-detail:first-child{padding-inline-start:0}.action-hero-detail:last-child{border-inline-end:0}.action-hero-detail .ui-icon{width:19px;height:19px;color:var(--blue)}.action-hero-detail small{display:block;font-size:7.5px;color:#718197;margin-bottom:2px}.action-hero-detail strong{display:block;font-size:9.5px;color:var(--ink)}.action-hero-brand{position:absolute;z-index:3;right:18px;top:13px;bottom:13px;width:185px;border-left:1px solid #ffffff88;display:flex;flex-direction:column;align-items:center;justify-content:center;color:#fff;text-align:center}.action-hero-brand img{max-width:86px;max-height:56px;object-fit:contain;filter:drop-shadow(0 3px 5px rgba(0,0,0,.12))}.action-hero-brand b{font-size:14px;letter-spacing:.06em;margin-top:4px}.action-hero-brand small{font-size:8px;line-height:1.3;color:#eaf3ff;margin-top:3px}
    [dir=rtl] .action-hero-main{padding:13px 18px 12px 300px}[dir=rtl] .action-hero-bg{inset:0 42% 0 0}[dir=rtl] .action-hero-brand{right:auto;left:18px;border-left:0;border-right:1px solid #ffffff88}
    .action-command{display:grid;grid-template-columns:minmax(0,1fr) auto;align-items:center;gap:18px;padding:13px 17px;margin-bottom:10px;border-inline-start:4px solid {{ $urgentTotal ? 'var(--red)' : 'var(--green)' }};background:linear-gradient(90deg,#f2fff8,#fff)}.action-command-main{display:flex;align-items:center;gap:12px}.action-command-mark{width:38px;height:38px;border-radius:50%;display:grid;place-items:center;font-size:17px;font-weight:900;background:{{ $urgentTotal ? '#fdebed' : '#e7f7ee' }};color:{{ $urgentTotal ? 'var(--red)' : 'var(--green)' }}}.action-command h3{font-size:13.5px;margin:0 0 4px}.action-command p{margin:0;color:var(--muted);font-size:8.5px}.action-command-meta{display:flex;gap:8px;flex-wrap:wrap;justify-content:flex-end}.action-summary{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:10px;margin-bottom:10px}.action-metric{display:grid;grid-template-columns:42px 1fr auto;gap:11px;align-items:center;padding:12px 13px;position:relative;overflow:hidden;transition:.18s ease;min-height:82px}.action-metric:before{content:"";position:absolute;inset-block:0;inset-inline-start:0;width:3px;background:var(--blue)}.action-metric.active{border-color:#9dbfe6;box-shadow:0 8px 24px rgba(20,117,209,.11)}.action-metric-icon{width:40px;height:40px;border-radius:50%;display:grid;place-items:center;background:#edf5ff;color:var(--blue)}.action-metric:nth-child(2) .action-metric-icon{background:#fff1e5;color:#e97800}.action-metric:nth-child(3) .action-metric-icon{background:#f3ebff;color:#7f3ee7}.action-metric:nth-child(4) .action-metric-icon{background:#fff0f2;color:var(--red)}.action-metric:nth-child(5) .action-metric-icon{background:#e9f8f1;color:var(--green)}.action-metric-icon .ui-icon{width:21px;height:21px}.action-metric span{font-size:9px;color:var(--muted)}.action-metric b{display:block;font-size:24px;margin:4px 0 2px;line-height:1}.action-metric small{display:block;color:var(--muted);font-size:8px;min-height:18px}.action-metric-arrow{width:27px;height:27px;border-radius:50%;background:#eff5fc;color:var(--blue);display:grid;place-items:center;font-size:15px;font-weight:900}.action-filters{padding:8px 10px;margin-bottom:10px;display:flex;align-items:center;gap:8px;overflow:auto}.action-filters strong{font-size:9px;white-space:nowrap;margin-inline-end:4px}.action-filter{display:inline-flex;align-items:center;gap:6px;padding:7px 10px;border-radius:8px;background:#f2f6fb;color:#52627a;font-size:8.5px;font-weight:800;white-space:nowrap}.action-filter.active{background:var(--navy);color:#fff}.action-filter em{font-style:normal;min-width:18px;height:18px;padding:0 5px;border-radius:9px;background:#ffffffd9;color:var(--navy);display:grid;place-items:center;font-size:8px}.action-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.action-panel{padding:0;overflow:hidden}.action-panel-head{padding:12px 14px 10px;border-bottom:1px solid #edf1f5;display:flex;justify-content:space-between;align-items:flex-start;gap:12px}.action-panel-head h3{font-size:12.5px;margin:0 0 3px}.action-panel-head p{font-size:8px;color:var(--muted);margin:0}.action-list{padding:0 14px}.action-item{padding:11px 0;border-bottom:1px solid #edf1f5;display:grid;grid-template-columns:minmax(0,1fr) auto;align-items:start;gap:13px}.action-item:last-child{border-bottom:0}.action-item-title{font-size:10px;font-weight:850}.action-item small{display:block;color:var(--muted);font-size:8px;margin-top:4px;line-height:1.45}.action-item-meta{display:flex;gap:5px;align-items:center;flex-wrap:wrap;margin-top:7px}.action-controls{display:flex;gap:6px;justify-content:flex-end;flex-wrap:wrap;max-width:280px}.action-form{display:grid;gap:7px;min-width:235px}.action-form textarea,.action-form input[type=file]{width:100%;border:1px solid #d8e0ea;border-radius:7px;padding:8px;font-size:9px;background:#fff}.action-form textarea{min-height:58px;resize:vertical}.action-disclosure{min-width:235px}.action-disclosure summary{list-style:none;cursor:pointer}.action-disclosure summary::-webkit-details-marker{display:none}.action-disclosure[open]{padding:9px;border:1px solid #e2e9f2;border-radius:9px;background:#f8fafc}.action-disclosure[open] summary{margin-bottom:8px}.action-empty{padding:22px 16px;text-align:center;color:var(--muted);font-size:9px}.action-empty strong{display:block;color:var(--ink);font-size:11px;margin-bottom:4px}.action-history{margin-top:10px;padding:0;overflow:hidden}.action-history-list{padding:0 14px}.action-history .action-item{grid-template-columns:minmax(0,1fr) auto}.action-resolution{margin-top:7px!important;padding:8px 9px;border-radius:7px;background:#f2f6fb;color:#40516a!important}.span-all{grid-column:1/-1}
    @media(max-width:1280px){.action-hero-main{padding-inline-end:255px}.action-hero-brand{width:155px}.action-summary{grid-template-columns:repeat(3,1fr)}}
    @media(max-width:950px){.action-hero-main{padding-inline:18px}.action-hero-bg,.action-hero-brand{display:none}.action-grid{grid-template-columns:1fr}.span-all{grid-column:auto}.action-summary{grid-template-columns:repeat(2,1fr)}}
    @media(max-width:700px){.portal-main{padding-inline:8px}.portal-topbar{margin-inline:-8px}.action-command{grid-template-columns:1fr}.action-command-meta{justify-content:flex-start}.action-summary{grid-template-columns:1fr}.action-item{grid-template-columns:1fr}.action-controls{justify-content:flex-start;max-width:none}.action-form,.action-disclosure{min-width:0;width:100%}.action-hero-detail{padding-inline:10px}.action-hero-copy h2{font-size:21px}}
</style>
@endpush

<section class="action-hero">
    <img class="action-hero-bg" src="{{ asset('images/unifco-home-hero-20260902.webp') }}" alt="">
    <div class="action-hero-main">
        <div class="action-hero-title">
            <div class="action-hero-title-icon">@include('customer.partials.icon',['name'=>'actions'])@if($total)<span class="action-hero-badge">{{ $total }}</span>@endif</div>
            <div class="action-hero-copy"><h2>{{ $ar ? 'الإجراءات المطلوبة منك' : 'Action Required From You' }}</h2><p>{{ $ar ? 'القرارات والمتابعات مرتبة حسب الأولوية وتاريخ الاستحقاق.' : 'Decisions and follow-ups prioritized by urgency and due date.' }}</p></div>
        </div>
        <div class="action-hero-details">
            <div class="action-hero-detail">@include('customer.partials.icon',['name'=>'dashboard'])<div><small>{{ $ar ? 'كود العميل' : 'Customer Code' }}</small><strong>{{ $customer->customer_code }}</strong></div></div>
            <div class="action-hero-detail">@include('customer.partials.icon',['name'=>'sites'])<div><small>{{ $ar ? 'الموقع' : 'Location' }}</small><strong>{{ $customer->city ?: ($ar ? 'الرياض، السعودية' : 'Riyadh, Saudi Arabia') }}</strong></div></div>
            <div class="action-hero-detail">@include('customer.partials.icon',['name'=>'inbox'])<div><small>{{ $ar ? 'البريد الإلكتروني' : 'Email' }}</small><strong>{{ $customer->email ?: auth()->user()->email }}</strong></div></div>
            <div class="action-hero-detail">@include('customer.partials.icon',['name'=>'profile'])<div><small>{{ $ar ? 'الجوال' : 'Phone' }}</small><strong>{{ $customer->phone ?: '—' }}</strong></div></div>
        </div>
    </div>
    <div class="action-hero-brand"><img src="{{ asset('images/unifco-logo.webp') }}" alt="UNIFCO"><b>UNIFCO</b><small>{{ $ar ? 'صيانة موثوقة لغدٍ أفضل' : 'Reliable Maintenance for a Better Tomorrow' }}</small></div>
</section>

<section class="portal-card action-command">
    <div class="action-command-main"><div class="action-command-mark">{{ $total ? '!' : '✓' }}</div><div>
        <h3>{{ $total ? ($ar ? 'لديك عناصر تحتاج المتابعة' : 'Items need your attention') : ($ar ? 'لا توجد إجراءات مطلوبة منك حاليًا' : 'No action is required from you right now') }}</h3>
        <p>{{ $total ? ($ar ? 'ابدأ بالعناصر العاجلة ثم تابع بقية القرارات المفتوحة.' : 'Start with urgent items, then continue through the remaining open decisions.') : ($ar ? 'كل الموافقات والاستلامات والمدفوعات والتجديدات محدثة.' : 'Approvals, work acceptance, payments and renewals are up to date.') }}</p>
    </div></div>
    <div class="action-command-meta">@if($urgentTotal)<span class="portal-pill red">{{ $urgentTotal }} {{ $ar ? 'عاجل' : 'urgent' }}</span>@endif @if($nextDueAt)<span class="portal-pill amber">{{ $ar ? 'أقرب استحقاق' : 'Next due' }} · {{ $nextDueAt->format('Y-m-d') }}</span>@endif <span class="portal-pill green">{{ $ar ? 'يشمل كامل حساب العميل' : 'Full customer account' }}</span></div>
</section>

<section class="action-summary" aria-label="{{ $ar ? 'ملخص الإجراءات' : 'Action summary' }}">
    <a class="portal-card action-metric {{ $actionType==='quotations'?'active':'' }}" href="{{ route('customer.actions',['type'=>'quotations']) }}"><span class="action-metric-icon">@include('customer.partials.icon',['name'=>'quotations'])</span><span><span>{{ $ar ? 'عروض تنتظر القرار' : 'Quotation decisions' }}</span><b>{{ $quotations->count() }}</b><small>{{ $ar ? 'مراجعة واعتماد أو طلب تعديل' : 'Review, approve or request revision' }}</small></span><i class="action-metric-arrow">›</i></a>
    <a class="portal-card action-metric {{ $actionType==='work'?'active':'' }}" href="{{ route('customer.actions',['type'=>'work']) }}"><span class="action-metric-icon">@include('customer.partials.icon',['name'=>'work-orders'])</span><span><span>{{ $ar ? 'أعمال تنتظر الاستلام' : 'Work acceptance' }}</span><b>{{ $workOrders->count() }}</b><small>{{ $acceptanceOver7Days ? ($acceptanceOver7Days.' '.($ar?'منذ أكثر من 7 أيام':'older than 7 days')) : ($ar?'لا يوجد استلام متأخر':'No delayed acceptance') }}</small></span><i class="action-metric-arrow">›</i></a>
    <a class="portal-card action-metric {{ $actionType==='invoices'?'active':'' }}" href="{{ route('customer.actions',['type'=>'invoices']) }}"><span class="action-metric-icon">@include('customer.partials.icon',['name'=>'invoices'])</span><span><span>{{ $ar ? 'الفواتير المستحقة' : 'Invoices Due' }}</span><b>{{ $invoices->count() }}</b><small>{{ $overdueInvoices ? ($overdueInvoices.' '.($ar?'متأخرة':'overdue')) : ($ar?'لا توجد فواتير متأخرة':'No overdue invoices') }}</small></span><i class="action-metric-arrow">›</i></a>
    <a class="portal-card action-metric {{ $actionType==='renewals'?'active':'' }}" href="{{ route('customer.actions',['type'=>'renewals']) }}"><span class="action-metric-icon">@include('customer.partials.icon',['name'=>'contracts'])</span><span><span>{{ $ar ? 'تجديدات العقود' : 'Contract renewals' }}</span><b>{{ $contracts->count() }}</b><small>{{ $renewalsWithin30Days ? ($renewalsWithin30Days.' '.($ar?'خلال 30 يومًا':'within 30 days')) : ($ar?'لا يوجد تجديد قريب':'No renewal within 30 days') }}</small></span><i class="action-metric-arrow">›</i></a>
    <a class="portal-card action-metric {{ $actionType==='messages'?'active':'' }}" href="{{ route('customer.actions',['type'=>'messages']) }}"><span class="action-metric-icon">@include('customer.partials.icon',['name'=>'inbox'])</span><span><span>{{ $ar ? 'الرسائل غير المقروءة' : 'Unread messages' }}</span><b>{{ $unread }}</b><small>{{ $ar ? 'رسائل ودعم مرتبط بالأعمال' : 'Work-related messages and support' }}</small></span><i class="action-metric-arrow">›</i></a>
</section>

<nav class="portal-card action-filters" aria-label="{{ $ar ? 'فئات الإجراءات' : 'Action categories' }}"><strong>{{ $ar ? 'فئات الإجراءات' : 'Action categories' }}</strong>@foreach($filters as $key => [$label,$count])<a class="action-filter {{ $actionType===$key?'active':'' }}" href="{{ route('customer.actions',['type'=>$key]) }}">{{ $label }} <em>{{ $count }}</em></a>@endforeach</nav>

<div class="action-grid">
    @if(in_array($actionType,['all','quotations'],true))
    <section class="portal-card action-panel"><header class="action-panel-head"><div><h3>{{ $ar ? 'عروض الأسعار بانتظار قرارك' : 'Quotations Awaiting Your Decision' }}</h3><p>{{ $ar ? 'الأقدم يظهر أولًا حتى لا تتعطل الموافقات.' : 'Oldest items appear first to prevent approval delays.' }}</p></div><span class="portal-pill">{{ $quotations->count() }}</span></header><div class="action-list">
        @forelse($quotations as $quotation)<article class="action-item"><div><div class="action-item-title">{{ $quotation->quotation_no }}</div><small>{{ number_format((float)$quotation->amount,2) }} {{ $quotation->currency }} · {{ $ar ? 'مراجعة' : 'Revision' }} {{ $quotation->revision_no ?: 0 }}</small><div class="action-item-meta"><span class="portal-pill amber">{{ str_replace('_',' ',$quotation->status) }}</span>@if($quotation->quotation_date)<span class="portal-pill">{{ $quotation->quotation_date->format('Y-m-d') }}</span>@endif</div></div><a class="portal-btn soft" href="{{ route('customer.section','quotations') }}">{{ $ar ? 'مراجعة العرض' : 'Review quotation' }}</a></article>@empty<div class="action-empty"><strong>{{ $ar ? 'لا توجد عروض تنتظر قرارًا' : 'No quotation decisions required' }}</strong>{{ $ar ? 'ستظهر العروض هنا فور إرسالها للمراجعة.' : 'New proposals will appear here when sent for review.' }}</div>@endforelse
    </div></section>
    @endif

    @if(in_array($actionType,['all','work'],true))
    <section class="portal-card action-panel"><header class="action-panel-head"><div><h3>{{ $ar ? 'أعمال مكتملة تنتظر الاستلام' : 'Completed Work Awaiting Acceptance' }}</h3><p>{{ $ar ? 'راجع تقرير الخدمة ثم اعتمد العمل أو اطلب إعادة زيارة.' : 'Review the service report, accept the work or request a revisit.' }}</p></div><span class="portal-pill">{{ $workOrders->count() }}</span></header><div class="action-list">
        @forelse($workOrders as $workOrder)@php($waitingDays=(int)($workOrder->completed_at?->copy()->startOfDay()->diffInDays(today()) ?? 0))<article class="action-item"><div><div class="action-item-title">{{ $workOrder->work_order_no }} · {{ $workOrder->asset?->name }}</div><small>{{ $ar ? 'اكتمل' : 'Completed' }} {{ $workOrder->completed_at?->format('Y-m-d H:i') }}</small><div class="action-item-meta">@if($waitingDays>=7)<span class="portal-pill red">{{ $ar ? 'ينتظر منذ' : 'Waiting' }} {{ $waitingDays }} {{ $ar ? 'أيام' : 'days' }}</span>@else<span class="portal-pill green">{{ $ar ? 'جاهز للاستلام' : 'Ready for acceptance' }}</span>@endif</div></div><div class="action-controls"><a class="portal-btn soft" href="{{ route('customer.work-orders.show',$workOrder) }}">{{ $ar ? 'تقرير الخدمة' : 'Service report' }}</a><form method="POST" action="{{ route('customer.work-acceptance.decide',$workOrder) }}">@csrf<input type="hidden" name="decision" value="ACCEPT"><button class="portal-btn green">{{ $ar ? 'اعتماد العمل' : 'Accept work' }}</button></form><details class="action-disclosure"><summary class="portal-btn red">{{ $ar ? 'طلب إعادة زيارة' : 'Request revisit' }}</summary><form class="action-form" method="POST" action="{{ route('customer.work-orders.revisit',$workOrder) }}">@csrf<textarea name="notes" required placeholder="{{ $ar ? 'وضح ما يحتاج إلى إعادة المعالجة' : 'Describe what needs to be revisited' }}"></textarea><button class="portal-btn red">{{ $ar ? 'إرسال الطلب' : 'Send request' }}</button></form></details></div></article>@empty<div class="action-empty"><strong>{{ $ar ? 'لا توجد أعمال تنتظر الاستلام' : 'No work orders awaiting acceptance' }}</strong>{{ $ar ? 'الأعمال المعتمدة أو الجاري تنفيذها لا تظهر هنا.' : 'Accepted or in-progress work does not appear here.' }}</div>@endforelse
    </div></section>
    @endif

    @if(in_array($actionType,['all','invoices'],true))
    <section class="portal-card action-panel"><header class="action-panel-head"><div><h3>{{ $ar ? 'فواتير تحتاج المتابعة' : 'Invoices Requiring Attention' }}</h3><p>{{ $ar ? 'الأكثر تأخرًا يظهر أولًا مع خيارات الاستفسار وإثبات السداد.' : 'Most overdue items appear first, with query and payment-proof options.' }}</p></div><span class="portal-pill {{ $overdueInvoices?'red':'' }}">{{ $invoices->count() }}</span></header><div class="action-list">
        @forelse($invoices as $invoice)@php($days=(int)($invoice->due_date?->copy()->startOfDay()->diffInDays(today(),false) ?? 0))<article class="action-item"><div><div class="action-item-title">{{ $invoice->document_no }}</div><small>{{ number_format((float)$invoice->open_amount,2) }} {{ $invoice->currency }} · {{ $ar ? 'الاستحقاق' : 'Due' }} {{ $invoice->due_date?->format('Y-m-d') }}</small><div class="action-item-meta">@if($days>0)<span class="portal-pill red">{{ $ar ? 'متأخرة منذ' : 'Overdue by' }} {{ $days }} {{ $ar ? 'أيام' : 'days' }}</span>@elseif($days===0)<span class="portal-pill red">{{ $ar ? 'مستحقة اليوم' : 'Due today' }}</span>@else<span class="portal-pill amber">{{ $ar ? 'تستحق خلال' : 'Due in' }} {{ abs($days) }} {{ $ar ? 'أيام' : 'days' }}</span>@endif</div></div><div class="action-controls"><a class="portal-btn soft" href="{{ route('customer.section','invoices') }}">{{ $ar ? 'عرض الفواتير' : 'View invoices' }}</a><details class="action-disclosure"><summary class="portal-btn">{{ $ar ? 'استفسار أو إثبات سداد' : 'Query or payment proof' }}</summary><form class="action-form" method="POST" action="{{ route('customer.invoices.query',$invoice) }}">@csrf<textarea name="notes" required placeholder="{{ $ar ? 'اكتب الاستفسار أو توضيح الدفعة' : 'Question or payment clarification' }}"></textarea><button class="portal-btn">{{ $ar ? 'إرسال الاستفسار' : 'Send query' }}</button></form><form class="action-form" method="POST" enctype="multipart/form-data" action="{{ route('customer.invoices.payment-proof',$invoice) }}">@csrf<input type="file" name="proof" accept=".pdf,.jpg,.jpeg,.png" required><textarea name="notes" placeholder="{{ $ar ? 'مرجع الدفعة' : 'Payment reference' }}"></textarea><button class="portal-btn green">{{ $ar ? 'رفع إثبات السداد' : 'Upload payment proof' }}</button></form></details></div></article>@empty<div class="action-empty"><strong>{{ $ar ? 'لا توجد فواتير تحتاج المتابعة' : 'No invoices require attention' }}</strong>{{ $ar ? 'لا توجد مبالغ مفتوحة تستحق خلال 14 يومًا.' : 'No open amounts are due within 14 days.' }}</div>@endforelse
    </div></section>
    @endif

    @if(in_array($actionType,['all','renewals'],true))
    <section class="portal-card action-panel"><header class="action-panel-head"><div><h3>{{ $ar ? 'عقود تقترب من التجديد' : 'Contracts Approaching Renewal' }}</h3><p>{{ $ar ? 'العقد الأقرب للانتهاء يظهر أولًا.' : 'The nearest contract expiry appears first.' }}</p></div><span class="portal-pill">{{ $contracts->count() }}</span></header><div class="action-list">
        @forelse($contracts as $contract)@php($daysLeft=(int)today()->diffInDays($contract->ends_on,false))<article class="action-item"><div><div class="action-item-title">{{ $contract->contract_no }} · {{ $contract->title }}</div><small>{{ $ar ? 'ينتهي' : 'Ends' }} {{ $contract->ends_on?->format('Y-m-d') }}</small><div class="action-item-meta"><span class="portal-pill {{ $daysLeft<=30?'red':'amber' }}">{{ $daysLeft<0 ? ($ar?'منتهي':'Expired') : ($daysLeft.' '.($ar?'يومًا متبقيًا':'days remaining')) }}</span></div></div><details class="action-disclosure"><summary class="portal-btn">{{ $ar ? 'طلب التجديد' : 'Request renewal' }}</summary><form class="action-form" method="POST" action="{{ route('customer.contracts.renewal-request',$contract) }}">@csrf<textarea name="notes" placeholder="{{ $ar ? 'متطلبات التجديد أو التغييرات المطلوبة' : 'Renewal requirements or requested changes' }}"></textarea><button class="portal-btn">{{ $ar ? 'إرسال طلب التجديد' : 'Send renewal request' }}</button></form></details></article>@empty<div class="action-empty"><strong>{{ $ar ? 'لا توجد عقود تستحق التجديد قريبًا' : 'No contract renewals due soon' }}</strong>{{ $ar ? 'ستظهر العقود النشطة قبل انتهائها بستين يومًا.' : 'Active contracts appear here 60 days before expiry.' }}</div>@endforelse
    </div></section>
    @endif

    @if(in_array($actionType,['all','messages'],true))
    <section class="portal-card action-panel {{ $actionType==='all'?'span-all':'' }}"><header class="action-panel-head"><div><h3>{{ $ar ? 'الرسائل والمتابعات' : 'Messages & Follow-ups' }}</h3><p>{{ $ar ? 'المحادثات المرتبطة بطلبات وأعمال العميل.' : 'Conversations connected to customer requests and work.' }}</p></div><span class="portal-pill {{ $unread?'red':'green' }}">{{ $unread }}</span></header><div class="action-list"><article class="action-item"><div><div class="action-item-title">{{ $unread ? ($ar ? 'لديك رسائل غير مقروءة' : 'You have unread messages') : ($ar ? 'لا توجد رسائل جديدة' : 'No new messages') }}</div><small>{{ $ar ? 'افتح صندوق الوارد لمتابعة فريق يونيفكو والرد ضمن سياق العمل.' : 'Open the inbox to follow up with UNIFCO and reply within the work context.' }}</small></div><a class="portal-btn {{ $unread?'red':'soft' }}" href="{{ route('customer.inbox') }}">{{ $ar ? 'فتح صندوق الوارد' : 'Open inbox' }}</a></article></div></section>
    @endif
</div>

<section class="portal-card action-history"><header class="action-panel-head"><div><h3>{{ $ar ? 'الإجراءات المرسلة' : 'Submitted Actions' }}</h3><p>{{ $ar ? 'سجل طلباتك وقراراتك وحالة معالجة يونيفكو لها.' : 'A record of your requests, decisions and UNIFCO resolution status.' }}</p></div><span class="portal-pill">{{ $submittedActions->count() }}</span></header><div class="action-history-list">
    @forelse($submittedActions as $action)<article class="action-item"><div><div class="action-item-title">{{ str_replace('_',' ',$action->action_type) }}</div><small>{{ $action->submitted_at?->format('Y-m-d H:i') }}@if($action->notes) · {{ $action->notes }}@endif</small>@if($action->resolution_notes)<small class="action-resolution"><strong>{{ $ar ? 'رد يونيفكو:' : 'UNIFCO:' }}</strong> {{ $action->resolution_notes }}@if($action->resolved_at) · {{ $action->resolved_at->format('Y-m-d H:i') }}@endif</small>@endif</div><span class="portal-pill {{ $action->status==='OPEN'?'amber':'green' }}">{{ str_replace('_',' ',$action->status) }}</span></article>@empty<div class="action-empty"><strong>{{ $ar ? 'لا توجد إجراءات مرسلة بعد' : 'No submitted actions yet' }}</strong>{{ $ar ? 'ستظهر هنا الاستفسارات وإثباتات السداد وطلبات التجديد وإعادة الزيارة.' : 'Queries, payment proofs, renewal and revisit requests will appear here.' }}</div>@endforelse
</div></section>

@include('customer.partials.portal-shell-close')