@include('customer.partials.portal-shell-open', [
    'activeSection' => 'actions',
    'pageTitle' => app()->getLocale() === 'ar' ? 'الإجراءات المطلوبة' : 'Action Required From You',
    'pageDescription' => app()->getLocale() === 'ar' ? 'كل القرارات والمتابعات التي تنتظر العميل.' : 'Every decision and follow-up waiting for the customer.',
])

@push('late-styles')
<style>
    .action-summary{display:grid;grid-template-columns:repeat(5,1fr);gap:10px;margin-bottom:14px}.action-metric{padding:14px}.action-metric span{font-size:9px;color:var(--muted)}.action-metric b{display:block;font-size:24px;margin-top:7px}.action-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.action-item{padding:11px 0;border-bottom:1px solid #edf1f5;display:flex;justify-content:space-between;gap:12px}.action-item:last-child{border-bottom:0}.action-item b{font-size:10px}.action-item small{display:block;color:var(--muted);font-size:9px;margin-top:3px}.action-form{display:grid;gap:6px;min-width:220px}.action-form textarea,.action-form input[type=file]{width:100%;border:1px solid #d8e0ea;border-radius:7px;padding:7px;font-size:9px;background:#fff}.action-form textarea{min-height:48px}.action-history{margin-top:12px}.all-clear{padding:36px;text-align:center;margin-bottom:14px}.all-clear-mark{width:48px;height:48px;border-radius:50%;background:#e7f7ee;color:var(--green);display:grid;place-items:center;margin:0 auto 10px;font-size:22px;font-weight:900}.all-clear h2{font-size:16px;margin:0 0 5px}.all-clear p{margin:0;color:var(--muted);font-size:10px}@media(max-width:950px){.action-summary{grid-template-columns:repeat(2,1fr)}.action-grid{grid-template-columns:1fr}}@media(max-width:520px){.action-summary{grid-template-columns:1fr}.action-item{flex-direction:column}.action-form{min-width:0}}
</style>
@endpush

<div class="portal-page-head"><div><h2>{{ app()->getLocale()==='ar' ? 'مطلوب منك الآن' : 'Action Required From You' }}</h2><p>{{ app()->getLocale()==='ar' ? 'مرتبة حسب الأولوية وتاريخ الاستحقاق.' : 'Prioritized by urgency and due date.' }}</p></div><span class="portal-pill {{ $total ? 'red' : 'green' }}">{{ $total }} {{ app()->getLocale()==='ar' ? 'إجراء مفتوح' : 'open actions' }}</span></div>

@if($total === 0)
    <section class="portal-card all-clear"><div class="all-clear-mark">✓</div><h2>{{ app()->getLocale()==='ar' ? 'لا توجد إجراءات مطلوبة منك حاليًا' : 'No action is required from you right now' }}</h2><p>{{ app()->getLocale()==='ar' ? 'سنظهر هنا أي موافقة أو استلام أو دفعة أو تجديد يحتاج متابعتك.' : 'Approvals, work acceptance, payments and renewals will appear here when needed.' }}</p></section>
@else
    <section class="action-summary">
        <div class="portal-card action-metric"><span>Quotations</span><b>{{ $quotations->count() }}</b></div><div class="portal-card action-metric"><span>Work Acceptance</span><b>{{ $workOrders->count() }}</b></div><div class="portal-card action-metric"><span>Invoices Due</span><b>{{ $invoices->count() }}</b></div><div class="portal-card action-metric"><span>Contract Renewals</span><b>{{ $contracts->count() }}</b></div><div class="portal-card action-metric"><span>Unread Messages</span><b>{{ $unread }}</b></div>
    </section>
    <div class="action-grid">
        <section class="portal-card portal-panel"><h3>Quotations Awaiting Your Decision</h3>@forelse($quotations as $quotation)<div class="action-item"><div><b>{{ $quotation->quotation_no }} · {{ number_format((float)$quotation->amount,2) }} {{ $quotation->currency }}</b><small>{{ str_replace('_',' ',$quotation->status) }}</small></div><a class="portal-btn soft" href="{{ route('customer.section','quotations') }}">Review quotation</a></div>@empty<div class="portal-empty">No quotation decisions required.</div>@endforelse</section>
        <section class="portal-card portal-panel"><h3>Completed Work Awaiting Acceptance</h3>@forelse($workOrders as $workOrder)<div class="action-item"><div><b>{{ $workOrder->work_order_no }} · {{ $workOrder->asset?->name }}</b><small>Completed {{ $workOrder->completed_at?->diffForHumans() }}</small></div><div class="action-form"><a class="portal-btn soft" href="{{ route('customer.work-orders.show',$workOrder) }}">View service report</a><form method="POST" action="{{ route('customer.work-acceptance.decide',$workOrder) }}">@csrf<input type="hidden" name="decision" value="ACCEPT"><button class="portal-btn green">Accept Work</button></form><form class="action-form" method="POST" action="{{ route('customer.work-orders.revisit',$workOrder) }}">@csrf<textarea name="notes" required placeholder="What needs to be revisited?"></textarea><button class="portal-btn red">Request Revisit</button></form></div></div>@empty<div class="portal-empty">No work orders awaiting acceptance.</div>@endforelse</section>
        <section class="portal-card portal-panel"><h3>Invoices Requiring Attention</h3>@forelse($invoices as $invoice)<div class="action-item"><div><b>{{ $invoice->document_no }} · {{ number_format((float)$invoice->open_amount,2) }} {{ $invoice->currency }}</b><small>Due {{ $invoice->due_date?->format('Y-m-d') }}</small></div><div class="action-form"><form class="action-form" method="POST" action="{{ route('customer.invoices.query',$invoice) }}">@csrf<textarea name="notes" required placeholder="Question or payment clarification"></textarea><button class="portal-btn">Send Query</button></form><form class="action-form" method="POST" enctype="multipart/form-data" action="{{ route('customer.invoices.payment-proof',$invoice) }}">@csrf<input type="file" name="proof" accept=".pdf,.jpg,.jpeg,.png" required><textarea name="notes" placeholder="Payment reference"></textarea><button class="portal-btn green">Upload Payment Proof</button></form></div></div>@empty<div class="portal-empty">No invoices require attention.</div>@endforelse</section>
        <section class="portal-card portal-panel"><h3>Contracts Approaching Renewal</h3>@forelse($contracts as $contract)<div class="action-item"><div><b>{{ $contract->contract_no }} · {{ $contract->title }}</b><small>Ends {{ $contract->ends_on?->format('Y-m-d') }}</small></div><form class="action-form" method="POST" action="{{ route('customer.contracts.renewal-request',$contract) }}">@csrf<textarea name="notes" placeholder="Renewal requirements or changes"></textarea><button class="portal-btn">Request Renewal</button></form></div>@empty<div class="portal-empty">No contract renewals due soon.</div>@endforelse</section>
    </div>
@endif

<section class="portal-card portal-panel action-history"><h3>Submitted Actions</h3>@forelse($submittedActions as $action)<div class="action-item"><div><b>{{ str_replace('_',' ',$action->action_type) }}</b><small>{{ $action->submitted_at?->format('Y-m-d H:i') }} · {{ $action->notes ?: 'No note' }}</small>@if($action->resolution_notes)<small><strong>UNIFCO:</strong> {{ $action->resolution_notes }}</small>@endif</div><span class="portal-pill {{ $action->status==='OPEN'?'amber':'green' }}">{{ $action->status }}</span></div>@empty<div class="portal-empty">No submitted actions yet.</div>@endforelse</section>

@include('customer.partials.portal-shell-close')
