@extends('layouts.app')
@section('title',($mode==='create'?'New User':'Edit User').' | UNIFCO Platform')
@section('heading',$mode==='create'?'New User':'Edit User')
@section('content')
<style>.form-shell{max-width:900px}.form-hero{background:linear-gradient(135deg,#132137,#1e315b);color:#fff;border-radius:16px;padding:20px 22px;margin-bottom:14px}.form-hero h1{margin:0 0 5px;font-size:22px}.form-hero p{margin:0;color:#dce5f2;font-size:12px}.form-card{background:#fff;border:1px solid #e3e8ef;border-radius:13px;padding:18px}.form-grid2{display:grid;grid-template-columns:1fr 1fr;gap:12px}.form-card label{font-size:10px;font-weight:700;color:#607086}.form-card input,.form-card select{width:100%}.form-actions{display:flex;gap:8px;margin-top:16px}.help{font-size:9px;color:#8190a4}@media(max-width:700px){.form-grid2{grid-template-columns:1fr}}</style>
<div class="form-shell"><section class="form-hero"><h1>{{ $mode==='create'?'Create New User':'Edit '.$managedUser->name }}</h1><p>Assign identity, role, organization, employee linkage and account status.</p></section>@if($errors->any())<div class="error">{{ $errors->first() }}</div>@endif<form class="form-card" method="POST" action="{{ $mode==='create'?route('admin.users.store'):route('admin.users.update',$managedUser) }}">@csrf @if($mode==='edit')@method('PUT')@endif<div class="form-grid2"><label>Name<input name="name" value="{{ old('name',$managedUser->name) }}" required></label><label>Email<input type="email" name="email" value="{{ old('email',$managedUser->email) }}" required></label><label>Role<select name="role" required>@foreach($roles as $r)<option value="{{ $r }}" @selected(old('role',$managedUser->role ?: 'TECHNICIAN')===$r)>{{ $r }}</option>@endforeach</select></label><label>Status<select name="status" required>@foreach($statuses as $s)<option value="{{ $s }}" @selected(old('status',$managedUser->status ?: 'ACTIVE')===$s)>{{ $s }}</option>@endforeach</select></label><label>Organization<select name="organization_id"><option value="">None</option>@foreach($organizations as $org)<option value="{{ $org->id }}" @selected((string)old('organization_id',$managedUser->organization_id)===(string)$org->id)>{{ $org->name }}</option>@endforeach</select></label><label>Employee<select name="employee_id"><option value="">Not linked</option>@foreach($employees as $employee)<option value="{{ $employee->id }}" @selected((string)old('employee_id',$managedUser->employee_id)===(string)$employee->id)>{{ $employee->employee_no }} · {{ $employee->name }}</option>@endforeach</select></label>@if($mode==='create')<label>Password<input type="password" name="password" required minlength="8"><span class="help">Minimum 8 characters.</span></label><label>Confirm Password<input type="password" name="password_confirmation" required minlength="8"></label>@endif</div><div class="form-actions"><button class="btn">{{ $mode==='create'?'Create User':'Save Changes' }}</button><a class="btn secondary" href="{{ route('workspace.show','users') }}">Cancel</a></div></form></div>
@endsection
