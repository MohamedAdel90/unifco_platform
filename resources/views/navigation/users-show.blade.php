@extends('layouts.app')
@section('title',$managedUser->name.' | User 360')
@section('heading','User 360')
@section('content')
@include('admin.system.operations-styles')
@php
$isCurrentUser=$managedUser->id===auth()->id();
$employee=$employees->firstWhere('id',$managedUser->employee_id);
$permissionGroups=$permissions->groupBy(fn($permission)=>str_contains($permission,'.')?strtoupper(explode('.',$permission,2)[0]):'GENERAL');
@endphp
<style>
.u360-hero{background:linear-gradient(135deg,#132137,#1e315b);color:#fff;border-radius:14px;padding:18px 20px;margin-bottom:12px;display:flex;justify-content:space-between;gap:16px;align-items:center;box-shadow:0 10px 28px rgba(19,33,55,.10)}.u360-id{display:flex;align-items:center;gap:12px}.u360-avatar{width:52px;height:52px;border-radius:50%;display:grid;place-items:center;background:#fff;color:#1e315b;font-size:18px;font-weight:900}.u360-hero small{font-size:8px;letter-spacing:.1em;text-transform:uppercase;color:#9fb0c7;font-weight:800}.u360-hero h1{margin:3px 0;font-size:21px}.u360-hero p{margin:0;color:#dce5f2;font-size:10px}.u360-actions{display:flex;gap:6px;flex-wrap:wrap}.u360-nav{display:flex;gap:6px;flex-wrap:wrap;background:#fff;border:1px solid #e4e9f0;border-radius:11px;padding:6px;margin-bottom:12px}.u360-nav a{padding:7px 10px;border-radius:8px;color:#516078;text-decoration:none;font-size:8.5px;font-weight:800}.u360-nav a:hover{background:#eef3fa;color:#1e315b}.u360-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.u360-card{background:#fff;border:1px solid #e4e9f0;border-radius:12px;padding:15px;box-shadow:0 4px 14px rgba(19,33,55,.03);scroll-margin-top:84px}.u360-card.wide{grid-column:1/-1}.u360-title{display:flex;justify-content:space-between;align-items:flex-start;gap:10px;margin-bottom:11px}.u360-title h3{margin:0;color:#132137;font-size:12px}.u360-title small{display:block;color:#7b899c;font-size:8px;margin-top:3px}.details{display:grid;grid-template-columns:repeat(2,1fr);gap:8px}.detail{background:#f8fafc;border:1px solid #edf1f5;border-radius:9px;padding:9px}.detail small{display:block;color:#8491a3;font-size:7.5px;text-transform:uppercase;letter-spacing:.04em}.detail b{display:block;margin-top:4px;font-size:10.5px;color:#17243c}.u360-list{display:grid;gap:6px}.u360-row{display:flex;justify-content:space-between;gap:10px;align-items:flex-start;padding:9px 10px;background:#fafbfd;border:1px solid #edf1f5;border-radius:9px;font-size:9.5px}.u360-row small{display:block;color:#7b899c;margin-top:3px;line-height:1.45}.tag{display:inline-flex;padding:4px 7px;border-radius:999px;background:#eef3fa;color:#1e315b;font-size:7.5px;font-weight:850;white-space:nowrap}.tag.good{background:#eaf7f0;color:#168453}.tag.warn{background:#fff6e6;color:#9b6100}.tag.bad{background:#fdeeee;color:#b42318}.security-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:7px;margin-bottom:10px}.security-grid .detail{min-height:66px}.action-grid{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:6px}.action-grid form,.action-grid button{width:100%}.danger{background:#a72b3e}.warn-btn{background:#a96b12}.success{background:#17734b}.note{padding:10px 11px;background:#f8fafc;border:1px solid #edf1f5;border-radius:9px;color:#68758a;font-size:8.5px;line-height:1.55;margin-top:9px}.permission-form{display:grid;grid-template-columns:1fr .8fr 1.2fr auto;gap:8px;align-items:end}.permission-form label{font-size:8px;color:#66758a;font-weight:750}.permission-tools{display:flex;justify-content:space-between;align-items:center;gap:10px;margin:14px 0 8px}.permission-search{width:min(360px,100%)}.permission-groups{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;max-height:420px;overflow:auto}.permission-group{border:1px solid #e5eaf1;border-radius:10px;overflow:hidden;background:#fff}.permission-group summary{list-style:none;padding:9px 10px;background:#f8fafc;font-size:9px;font-weight:850;color:#263652;cursor:pointer;display:flex;justify-content:space-between}.permission-group summary::-webkit-details-marker{display:none}.permission-group summary:after{content:'+'}.permission-group[open] summary:after{content:'−'}.permission-body{display:grid;gap:4px;padding:7px}.perm{padding:6px 7px;border-radius:7px;background:#fafbfd;color:#42506a;font-size:8.5px}.self-warning{padding:9px 10px;background:#fff7e8;border:1px solid #f1dfb0;color:#815800;border-radius:9px;font-size:8.5px;margin-top:9px}@media(max-width:980px){.u360-grid{grid-template-columns:1fr}.u360-card.wide{grid-column:auto}.security-grid{grid-template-columns:1fr 1fr}.action-grid{grid-template-columns:1fr 1fr}.permission-form{grid-template-columns:1fr}.permission-groups{grid-template-columns:1fr}.u360-hero{align-items:flex-start;flex-direction:column}}@media(max-width:560px){.details,.security-grid,.action-grid{grid-template-columns:1fr}.u360-nav{overflow:auto;flex-wrap:nowrap}.u360-nav a{white-space:nowrap}}
</style>
@if(session('status'))<div class="notice">{{ session('status') }}</div>@endif @if($errors->any())<div class="error">{{ $errors->first() }}</div>@endif
<section class="u360-hero"><div class="u360-id"><span class="u360-avatar">{{ strtoupper(substr($managedUser->name,0,1)) }}</span><div><small>Identity · Access · Security · Audit</small><h1>{{ $managedUser->name }}</h1><p>{{ $managedUser->email }} · {{ $assignedRoles->pluck('code')->join(' + ') ?: $managedUser->role }} · {{ $managedUser->status }} @if($isCurrentUser)· CURRENT ACCOUNT @endif</p></div></div><div class="u360-actions">@if(!$isCurrentUser)<form method="POST" action="{{ route('admin.impersonation.start',$managedUser) }}">@csrf<button class="btn danger" onclick="return confirm('Start read-only View as User session?')">View as User</button></form>@endif<a class="btn secondary" href="{{ route('admin.users.edit',$managedUser) }}">Edit User</a><a class="btn secondary" href="{{ route('workspace.show','users') }}">Back to Users</a></div></section>
<nav class="u360-nav"><a href="#overview">Overview</a><a href="#access">Access</a><a href="#security">Security</a><a href="#sessions">Sessions</a><a href="#permissions">Permissions</a><a href="#audit">Audit History</a></nav>
<div class="u360-grid">
<section class="u360-card wide" id="overview"><div class="u360-title"><div><h3>Identity Overview</h3><small>Authoritative user and employment identity.</small></div><span class="ops-status {{ $managedUser->status==='ACTIVE'?'success':'warning' }}">{{ $managedUser->status }}</span></div><div class="details"><div class="detail"><small>Name AR</small><b>{{ $managedUser->name_ar ?: '—' }}</b></div><div class="detail"><small>Name EN</small><b>{{ $managedUser->name_en ?: $managedUser->name }}</b></div><div class="detail"><small>Employee ID</small><b>{{ $employee?->employee_no ?? '—' }}</b></div><div class="detail"><small>Email</small><b>{{ $managedUser->email }}</b></div><div class="detail"><small>Mobile</small><b>{{ $managedUser->mobile ?: '—' }}</b></div><div class="detail"><small>User Type</small><b>{{ $managedUser->user_type ?? ($managedUser->customer_id?'EXTERNAL':'INTERNAL') }}</b></div><div class="detail"><small>Department</small><b>{{ $employee?->position?->department ?? '—' }}</b></div><div class="detail"><small>Job Title</small><b>{{ $employee?->position?->title ?? '—' }}</b></div></div></section>
<section class="u360-card" id="access"><div class="u360-title"><div><h3>Roles & Scopes</h3><small>Effective assignment sources.</small></div><span class="tag">{{ $assignedRoles->count() }} roles</span></div><div class="u360-list">@forelse($assignedRoles as $role)<div class="u360-row"><span><b>{{ $role->name_ar ?: $role->name_en }}</b><small>{{ $role->code }} · Granted {{ $role->pivot->granted_at ?: 'legacy' }} · By {{ $grantors[$role->pivot->granted_by] ?? 'System migration' }}</small></span><span class="tag {{ $role->pivot->is_primary?'good':'' }}">{{ $role->pivot->is_primary?'PRIMARY':'ADDITIONAL' }}</span></div>@empty<div class="note">Legacy role: {{ $managedUser->role }}</div>@endforelse</div><div class="u360-title" style="margin-top:13px"><div><h3>Scopes</h3><small>Data visibility boundaries.</small></div><span class="tag">{{ $assignedScopes->count() }}</span></div><div class="u360-list">@forelse($assignedScopes as $scope)<div class="u360-row"><span><b>{{ $scope->scope_type }} · {{ $scope->name ?: '#'.$scope->scope_id }}</b><small>By {{ $grantors[$scope->pivot->granted_by] ?? 'System migration' }} @if($scope->pivot->expires_at)· Expires {{ $scope->pivot->expires_at }}@endif</small></span><span class="tag">{{ $scope->pivot->source }}</span></div>@empty<div class="note">No active scope. Scope-aware resources are denied by default.</div>@endforelse</div></section>
<section class="u360-card" id="projects"><div class="u360-title"><div><h3>Project Assignments</h3><small>Operational project ownership and access derived from Project Team assignments.</small></div><span class="tag">{{ $projectAssignments->where('status','ACTIVE')->count() }} active</span></div><div class="u360-list">@forelse($projectAssignments as $assignment)<div class="u360-row"><span><b>{{ $assignment->project?->project_no }} · {{ $assignment->project?->name }}</b><small>{{ str_replace('_',' ',$assignment->project_role) }} · {{ str_replace('_',' ',$assignment->access_level) }} · {{ $assignment->starts_on?->format('d M Y') ?: 'Open start' }} → {{ $assignment->ends_on?->format('d M Y') ?: 'Open end' }}</small></span><span class="tag {{ $assignment->status==='ACTIVE'?'good':'' }}">{{ $assignment->status }}</span></div>@empty<div class="note">No project assignment recorded for this user.</div>@endforelse</div></section>
<section class="u360-card" id="security"><div class="u360-title"><div><h3>Security Summary</h3><small>Account protection and lifecycle controls.</small></div><span class="tag {{ $managedUser->locked_at?'bad':'good' }}">{{ $managedUser->locked_at?'LOCKED':'NORMAL' }}</span></div><div class="security-grid"><div class="detail"><small>Last Login</small><b>{{ $managedUser->last_login_at?->format('d M Y H:i') ?? 'Never' }}</b></div><div class="detail"><small>MFA</small><b>{{ str_replace('_',' ',$managedUser->mfa_status ?? 'NOT_CONFIGURED') }}</b></div><div class="detail"><small>Account Lock</small><b>{{ $managedUser->locked_at?'LOCKED':'UNLOCKED' }}</b></div><div class="detail"><small>Password</small><b>{{ $managedUser->force_password_change?'CHANGE REQUIRED':'NORMAL' }}</b></div></div><div class="action-grid">@foreach(['LOCK'=>'Lock','UNLOCK'=>'Unlock','REQUIRE_PASSWORD_CHANGE'=>'Require Password Change','CLEAR_PASSWORD_CHANGE'=>'Clear Password Flag','REVOKE_SESSIONS'=>'Revoke Sessions'] as $action=>$label)<form method="POST" action="{{ route('admin.users.security',$managedUser) }}" @if($action==='REVOKE_SESSIONS') onsubmit="return confirm('Revoke all active sessions for this user?')" @endif>@csrf<input type="hidden" name="action" value="{{ $action }}"><button class="btn {{ in_array($action,['LOCK','REVOKE_SESSIONS'])?'danger':'secondary' }}" @disabled(($isCurrentUser && $action==='LOCK') || ($action==='LOCK' && $managedUser->locked_at) || ($action==='UNLOCK' && !$managedUser->locked_at) || ($action==='REQUIRE_PASSWORD_CHANGE' && $managedUser->force_password_change) || ($action==='CLEAR_PASSWORD_CHANGE' && !$managedUser->force_password_change))>{{ $label }}</button></form>@endforeach</div>@if($isCurrentUser)<div class="self-warning">Your current administrator account is protected from self-lock, suspension and destructive session actions.</div>@endif<div class="note">MFA remains NOT CONFIGURED until the real enrollment provider is enabled. No security state is inferred or fabricated.</div></section>
<section class="u360-card"><div class="u360-title"><div><h3>Account Status & Password</h3><small>Lifecycle and recovery actions.</small></div></div><div class="u360-actions">@foreach(['ACTIVE'=>'Activate','INACTIVE'=>'Deactivate','SUSPENDED'=>'Suspend'] as $value=>$label)<form method="POST" action="{{ route('admin.users.status',$managedUser) }}">@csrf<input type="hidden" name="status" value="{{ $value }}"><button class="btn {{ $value==='ACTIVE'?'success':($value==='SUSPENDED'?'danger':'warn-btn') }}" @disabled($managedUser->status===$value || ($isCurrentUser && $value!=='ACTIVE'))>{{ $label }}</button></form>@endforeach</div><form method="POST" action="{{ route('admin.users.reset-password',$managedUser) }}" style="margin-top:10px" onsubmit="return confirm('Send a secure password reset invitation and revoke all existing sessions?')">@csrf<button class="btn">Send Password Reset Invitation</button></form><div class="note">The administrator never sees or chooses the user password. Reset uses an audited one-time link and revokes existing sessions.</div></section>
<section class="u360-card" id="sessions"><div class="u360-title"><div><h3>Active Sessions</h3><small>Device, browser, IP and recent activity.</small></div><span class="tag">{{ $activeSessions->where('status','ACTIVE')->count() }} active</span></div><div class="u360-list">@forelse($activeSessions as $session)<div class="u360-row"><span><b>{{ $session->device ?: 'Unknown device' }} · {{ $session->browser ?: 'Unknown browser' }}</b><small>{{ $session->ip_address ?: 'IP unavailable' }} · Login {{ $session->login_at }} · Last activity {{ $session->last_activity_at }}</small></span><span>@if($session->status==='ACTIVE' && $session->session_id!==request()->session()->getId())<form method="POST" action="{{ route('admin.users.sessions.revoke',[$managedUser,$session->id]) }}">@csrf<button class="btn danger" onclick="return confirm('Force logout this session?')">Force Logout</button></form>@else<span class="tag">{{ $session->status }}</span>@endif</span></div>@empty<div class="note">No recorded sessions.</div>@endforelse</div></section>
<section class="u360-card"><div class="u360-title"><div><h3>API Tokens</h3><small>Programmatic credentials issued to this user.</small></div><span class="tag">{{ $apiTokens->count() }}</span></div><div class="u360-list">@forelse($apiTokens as $token)<div class="u360-row"><span><b>{{ $token->name }}</b><small>{{ $token->isActive()?'Active':'Revoked / Expired' }} · Last used {{ $token->last_used_at?->format('d M Y H:i') ?? 'Never' }}</small></span>@if($token->isActive())<form method="POST" action="{{ route('admin.users.api-tokens.revoke',[$managedUser,$token]) }}" onsubmit="return confirm('Revoke this API token?')">@csrf<button class="btn danger">Revoke</button></form>@else<span class="tag">INACTIVE</span>@endif</div>@empty<div class="note">No API tokens issued.</div>@endforelse</div></section>
<section class="u360-card wide" id="permissions"><div class="u360-title"><div><h3>User-specific Permission Exceptions</h3><small>Exceptions should be rare, explicit and justified.</small></div><span class="tag">{{ $overrides->count() }} overrides</span></div><form class="permission-form" method="POST" action="{{ route('admin.users.permission',$managedUser) }}">@csrf<label>Permission Code<input name="permission_code" placeholder="e.g. inventory.stock.move" required></label><label>Effect<select name="effect"><option value="ALLOW">Allow</option><option value="DENY">Deny</option><option value="INHERIT">Remove exception / inherit roles</option></select></label><label>Reason<input name="reason" placeholder="Required for an exception"></label><button class="btn">Apply Exception</button></form><div class="u360-list" style="margin-top:10px">@forelse($overrides as $override)<div class="u360-row"><span>{{ $override->permission_code }}<small>{{ $override->reason ?: 'No legacy reason recorded' }}</small></span><span class="tag {{ $override->allowed?'good':'bad' }}">{{ $override->allowed?'ALLOW':'DENY' }}</span></div>@empty<div class="note">No user-specific exceptions. Effective access follows Roles + Scope.</div>@endforelse</div><div class="permission-tools"><h3 style="margin:0;color:#132137;font-size:11px">Effective Permissions <span class="tag">{{ $permissions->count() }}</span></h3><input class="permission-search" id="permission-search" type="search" placeholder="Search permissions…"></div><div class="permission-groups" id="permission-groups">@forelse($permissionGroups as $group=>$groupPermissions)<details class="permission-group"><summary><span>{{ $group }}</span><span>{{ $groupPermissions->count() }}</span></summary><div class="permission-body">@foreach($groupPermissions as $permission)<div class="perm" data-permission>{{ $permission }}</div>@endforeach</div></details>@empty<div class="note">No effective permissions.</div>@endforelse</div></section>
<section class="u360-card wide" id="audit"><div class="u360-title"><div><h3>Recent Audit History</h3><small>Append-only changes related to this identity.</small></div><a class="btn secondary" href="{{ route('admin.audit.index') }}">Open Full Audit Trail</a></div><div class="u360-list">@forelse($auditTimeline as $event)<div class="u360-row"><span><b>{{ $event->action }}</b><small>{{ $event->created_at }} · {{ $event->ip_address ?? 'IP not recorded' }} @if($event->reason)· {{ $event->reason }}@endif</small></span><span class="tag">AUDITED</span></div>@empty<div class="note">No recorded audit events for this user.</div>@endforelse</div></section>
</div>
<script>(()=>{const q=document.getElementById('permission-search');q?.addEventListener('input',()=>{const term=q.value.toLowerCase().trim();document.querySelectorAll('[data-permission]').forEach(x=>x.style.display=!term||x.textContent.toLowerCase().includes(term)?'block':'none');document.querySelectorAll('.permission-group').forEach(g=>{const visible=[...g.querySelectorAll('[data-permission]')].some(x=>x.style.display!=='none');g.style.display=visible?'block':'none';if(term&&visible)g.open=true;});});})();</script>
@endsection
